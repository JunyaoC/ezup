---
description: Is this session being shared, where do the bytes go, and why
---

Run `ezup share status` with the Bash tool and relay its output faithfully —
the `why` line verbatim, since explaining exactly why sharing is on or off is
the point of the feature. Do not run any other command.
